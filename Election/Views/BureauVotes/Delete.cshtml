@model Election.Models.BureauVote
@{
    Layout = "~/Views/Shared/_admin.cshtml";
}

@{
    ViewBag.Title = "Suppression";
}

<h2  class="alert alert-danger">Supprimer</h2>
<div class="container">
    <div class="row">
        
        <div class="col-sm-8">
            <h3 class="alert alert">Etes vous sur de vouloir continuer la suppression?</h3>
            <div>
                <h4>BureauVote</h4>
                <hr />
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.code)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.code)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(model => model.lieu)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.lieu)
                    </dd>

                </dl>

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-actions no-color">
                        <input type="submit" value="Supprimer" class="btn btn-danger" /> |
                        @Html.ActionLink("Back to List", "Index")
                    </div>
                }
            </div>

        </div>
    </div>
</div>
